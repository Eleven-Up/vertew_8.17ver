# 프로젝트 기획 브리프: 시장 상인용 홀로그램 대화 서비스

## 1. 개요
- **컨셉**: 시장 상인을 대신해 손님과 음성으로 대화하는 홀로그램 캐릭터 서비스
- **배경**: 'street vendors를 위한 기술 창업' 대회 출품작
- **동작 요약**: 손님이 홀로그램 디스플레이 앞에서 화면을 탭하고 말을 걸면,
  친근한 상인 캐릭터가 표정·몸짓과 함께 음성으로 응답한다.

## 2. 하드웨어 / 팀 구성
- 라즈베리파이 5 (4GB) 사용
- HW팀: 홀로그램 디스플레이 담당 (HDMI 입력으로 영상 수신 전제)
- SW팀: 대화 서비스 + 웹 화면 담당 (각 팀 Pi 1대씩, GitHub로 코드 공유)
- 마이크/스피커: Pi에 USB 연결
- 화면 출력: 웹페이지를 Chromium 풀스크린으로 띄움

## 3. 핵심 결정사항
- **시작 방식**: 화면 탭 → push-to-talk (소음 오작동/불필요한 API 호출 방지)
- **동시 사용자**: 데모는 1명. 다중 사용자는 확장 과제.
- **대화 길이**: 짧은 대화 (시장 흥정 수준)
- **캐릭터**:
  - MVP는 미리 제작된 2D 캐릭터 1개
  - 친근한 상인 페르소나, 세부 성격은 프롬프트로 설정 가능
  - 동작 수준: 2D 표정 + 몸짓 (3D 제외, "어색하지 않게")
  - 후보 기술: Live2D(립싱크·idle 모션 내장, 추천) 또는 PNG 스프라이트
- **음성**: 브라우저 Web Speech API (STT/TTS 모두 무료)
  - TTS는 브라우저 로컬 합성 (인터넷 불필요)
  - STT는 크롬이 구글 서버로 오디오 전송 (인터넷 사용)
- **LLM**: Google Gemini Flash (무료 등급)
- **지식(RAG)**: 상인이 관리자 페이지에서 매장/상품 정보 입력 → 프롬프트에 반영
- **언어**: 데모는 영어. 이후 한국어 → 일본어/중국어 확장.
- **제약**: 욕설 금지, 통상적인 시장 상인 수준의 응대
- **예산**: 최대한 무료 (Gemini 무료등급, 브라우저 STT/TTS, Pi 호스팅)

## 4. 네트워크 절약 설계 (와이파이 약함 대비)
1. UI·캐릭터·TTS는 전부 Pi 로컬에서 처리 → 인터넷 0
2. TTS는 브라우저 로컬 합성
3. 인터넷 사용처는 음성인식(STT) + Gemini 호출(텍스트) 둘뿐
4. Gemini 호출 최소화: 최근 대화 몇 턴만 유지, 시스템 프롬프트 간결, Flash 모델
5. Gemini가 답변+감정+제스처를 한 번에 JSON으로 반환 (추가 호출 없이 애니메이션 결정)
6. STT는 모듈로 분리 → 추후 로컬 STT(Vosk)로 교체 가능하게

## 5. 아키텍처
[브라우저(Pi, Chromium 풀스크린): 캐릭터 표시 + STT + TTS]
   ↕ localhost (인터넷 X)
[FastAPI 서버(Pi): 대화관리 + 프롬프트 구성 + SQLite + 관리자페이지]
   ↕ 인터넷 (텍스트만)
[Gemini API: LLM 추론]
→ Pi의 HDMI 출력 → 홀로그램 디스플레이(HW팀)

## 6. 대화 흐름
화면 탭 → 마이크 ON → 손님 발화
→ STT 텍스트 변환(브라우저) → 서버 전송
→ [페르소나 + 매장정보 + 최근 대화] 프롬프트 구성 → Gemini 호출
→ { "text": "...", "emotion": "happy", "gesture": "wave" } 반환
→ 브라우저가 캐릭터 표정/몸짓 + 음성 출력(TTS)

## 7. 기술 스택
- 서버: Python + FastAPI (WebSocket)
- LLM: Google Gemini Flash
- STT/TTS: 브라우저 Web Speech API
- DB: SQLite (매장정보 + 대화로그)
- 캐릭터: Live2D 또는 PNG 스프라이트
- 키오스크: Chromium 풀스크린
- 관리자: FastAPI 내 /admin 페이지
- (선택) 외부 접속: Cloudflare Tunnel (무료)

## 8. 레포 구조 (제안)
vertew/
├── backend/
│   ├── main.py            # FastAPI 진입점
│   ├── llm.py             # Gemini 호출 + 프롬프트
│   ├── db.py              # SQLite
│   ├── admin.py           # 관리자 라우트
│   └── requirements.txt
├── frontend/
│   ├── index.html         # 손님용 캐릭터 화면
│   ├── admin.html         # 상인용 정보입력 화면
│   ├── js/
│   │   ├── speech.js      # STT/TTS (모듈 분리)
│   │   ├── character.js   # 캐릭터 애니메이션 제어
│   │   └── chat.js        # 서버 통신
│   └── assets/            # 캐릭터 모델/이미지
├── scripts/
│   └── kiosk.sh           # Chromium 풀스크린 자동실행
└── README.md

## 9. 단계별 개발 계획 (~8월 마감)
1. 뼈대: FastAPI + Gemini 텍스트 채팅 (타이핑→응답)
2. 음성: 브라우저 STT/TTS 연결 (탭→말하기→음성응답)
3. 캐릭터: 2D 캐릭터 + emotion/gesture 매핑
4. 지식: 관리자 페이지에서 매장/상품 정보 입력 → 프롬프트 반영
5. 마감: Pi 키오스크 모드, HDMI 출력 확인, 영어 시연 다듬기

## 10. 확장 과제 (발표 시 '향후 계획')
- 다중 사용자 동시 이용
- 사용자 사진 → 맞춤형 캐릭터 생성 (Mii 스타일, VARCO 등 이미지 생성 API)
  - 주의: 생성된 임의 이미지의 자동 리깅/실시간 애니메이션은 GPU 필요, Pi 실시간 구동 어려움
- 다국어 (한/일/중)
- 완전 오프라인 버전 (로컬 LLM: Ollama+소형모델, 로컬 STT: Vosk, 로컬 TTS: Piper)

## 11. 준비물
- Google AI Studio에서 Gemini API 키 발급 (무료)
