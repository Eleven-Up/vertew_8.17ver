# Vertew Conversation_Server (FastAPI) dependencies

# Web server
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=12.0
python-multipart>=0.0.9

# Gemini Flash API (text-only LLM call)
google-genai>=0.3.0

# HTTP client (network timeout / retry boundary)
httpx>=0.27,<1.0

# Local, fully-offline speech-to-text (only used when STT_PROVIDER=local)
faster-whisper>=1.0

# Configuration / secrets
python-dotenv>=1.0

# ---- Testing ----
pytest>=8.0
hypothesis>=6.100
pytest-asyncio>=0.23
