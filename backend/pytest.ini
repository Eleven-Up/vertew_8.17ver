[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
asyncio_mode = auto

# Hypothesis settings are configured per-test; profiles can be registered in
# tests/conftest.py. Property tests run a minimum of 100 iterations.
